# Copy this file to .env (in core/app/) and adjust if your Postgres setup
# differs from the default docker-compose.yml. The app falls back to this
# exact default automatically even without a .env file.

DATABASE_URL=postgresql://netra_user:netra_password@localhost:5432/netra_cctv
